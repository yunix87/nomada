{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Depurar C++ (CodeLLDB)",
      "type": "lldb",
      "request": "launch",
      "program": "${fileDirname}/${fileBasenameNoExtension}.exe",
      "args": [],
      "cwd": "${fileDirname}",
      "console": "integratedTerminal"
    }
  ]
}